// src/components/common/PageTransition.jsx
// The route transition lives with the rest of the design system now; this
// re-export keeps the original import path working.
export { default } from "../fx/PageTransition";
